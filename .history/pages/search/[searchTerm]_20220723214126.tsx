import React from 'react'

const Search = () => {
  return (
    <div></div>
  )
}

export const getServerSideProps = asunc ({
    parms: { id}
}: {
    params: { id: string }
}
})

export default Search